<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>

  </ol>
  <%= will_paginate(@feed_items, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer, class: 'justify-content-center',
    params: {controller: :static_pages, action: :home }) %>
<% end %>

